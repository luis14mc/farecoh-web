/** Horizontal center of the right-stub code box (pixels, top-left origin). */
export const CODE_X = 1710;
/** Vertical center of the right-stub code box (pixels, top-left origin). */
export const CODE_Y = 148;
export const CODE_FONT_SIZE = 36;
/** Mask behind dynamic code to cover static PF-0001 in the template. */
export const CODE_MASK_WIDTH = 300;
export const CODE_MASK_HEIGHT = 58;

/** Top-left X of the QR target square inside the purple frame (pixels, top-left origin). */
export const QR_X = 1578;
/** Top-left Y of the QR target square (pixels, top-left origin). */
export const QR_Y = 248;
export const QR_SIZE = 264;
export const QR_PADDING = 8;
export const QR_RENDER_SCALE = 4;

export const MAX_PRINT_TICKETS_PER_REQUEST = 500;
