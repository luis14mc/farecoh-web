---
import AdminLayout from "@/layouts/AdminLayout.astro";
import { AdminPageHeader } from "@/components/admin/react/AdminPageHeader";
import { CheckinPanel } from "@/components/admin/react/CheckinPanel";
import { getPublicSupabaseConfig, isAuthConfigured, type UserProfile } from "@/lib/auth";
import { formatSiteDate } from "@/lib/locale";

const staffProfile = Astro.locals.staffProfile as UserProfile | undefined;
const authConfigured = isAuthConfigured(getPublicSupabaseConfig());
const currentDate = formatSiteDate(new Date(), { month: "short", day: "numeric", year: "numeric" });
---

<AdminLayout title="Control de acceso | FARECOH" description="Validación de ingreso de boletos físicos" active="checkin">
  <AdminPageHeader
    client:load
    title="Control de acceso"
    section="Check-in"
    description="Busque el código impreso del boleto físico para validar ingreso."
    currentDate={currentDate}
    staffProfile={staffProfile}
    authConfigured={authConfigured}
  />

  <div class="mx-auto w-full max-w-2xl space-y-6 px-4 py-6 pb-24 md:px-8 md:py-8 md:pb-8">
    <CheckinPanel client:load />
  </div>
</AdminLayout>
