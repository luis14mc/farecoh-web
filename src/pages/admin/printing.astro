---
import AdminLayout from "@/layouts/AdminLayout.astro";
import AdminPage from "@/components/admin/ui/AdminPage.astro";
import { AdminPageHeader } from "@/components/admin/react/AdminPageHeader";
import { PrintingPanel } from "@/components/admin/react/PrintingPanel";
import { getPublicSupabaseConfig, isAuthConfigured, type UserProfile } from "@/lib/auth";
import { getPinkFloydEvent } from "@/lib/events";
import { formatSiteDate } from "@/lib/locale";

const staffProfile = Astro.locals.staffProfile as UserProfile | undefined;
const authConfigured = isAuthConfigured(getPublicSupabaseConfig());
const currentDate = formatSiteDate(new Date(), { month: "short", day: "numeric", year: "numeric" });

const event = await getPinkFloydEvent();
---

<AdminLayout title="Impresión | FARECOH" description="Generación de PDFs imprimibles para boletos físicos" active="printing">
  <AdminPageHeader
    client:load
    title="Impresión"
    section="Operaciones"
    description="PDFs con plantilla Canva, código y QR para impresión física."
    currentDate={currentDate}
    staffProfile={staffProfile}
    authConfigured={authConfigured}
  />

  <AdminPage>
    <PrintingPanel client:load eventName={event.name} />
  </AdminPage>
</AdminLayout>
