---
import PageHeader from "@/components/admin/ui/PageHeader.astro";

interface Props {
  title: string;
  section: string;
  description?: string;
}

const props = Astro.props;
---

<PageHeader {...props} />
